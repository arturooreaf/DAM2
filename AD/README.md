# AD - Acceso a Datos

## Sesion 01 - Entorno y primer script (PHP)

Carpeta `curso-php/`:

- `index.php` - primer script: una isla de PHP dentro de HTML que imprime "Hola mundo desde PHP".
- `islas.php` - bucle `for` con sintaxis alternativa (`for: ... endfor;`) mezclado con HTML.

Se sirven desde `http://localhost/curso-php/` gracias a un junction que enlaza
`C:\xampp\htdocs\curso-php` con esta carpeta. No hay copias: es el mismo archivo.

## Material de estudio

- `php-de-cero-a-pro.md` - ruta de estudio por niveles con ejercicios + referencia de PHP.
- El resumen del entorno (que se instalo y por que) esta en `entorno-php-AD.pdf`, en el Escritorio.

## Pendiente

La Practica de la sesion 01: `formulario.php` y `resultado.php`.
